# The gold price proxy (server/index.js) uses the Yahoo Finance public chart API
# and needs NO API key. Copying this file is optional.
#
# Port for the backend proxy (used by the Vite dev proxy at http://localhost:3001)
PORT=3001
